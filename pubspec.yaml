name: affynity
description: A mobile-first collab platform for brands & creators
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.1
  swipable_stack: ^1.0.0
  flutter_animate: ^4.3.0
  flutter_staggered_animations: ^1.1.1
  flutter_form_builder: ^9.1.1
  cached_network_image: ^3.3.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true